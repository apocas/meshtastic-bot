# Example configuration for Meshtastic Bot

# The serial port for your Meshtastic device
# On Linux, this is typically /dev/ttyUSB0
# On macOS, it might be /dev/cu.usbmodem* or /dev/cu.usbserial*
# On Windows, it will be something like COM3
#PORT=/dev/ttyUSB0

# The path to the SQLite database file
#DB_PATH=seen_nodes.db

# Connection type: "serial" or "ip"
#CONNECTION_TYPE=serial

# IP address of the Meshtastic device (if using IP connection)
#DEVICE_IP=192.168.1.10

# Action Configuration
# Node cleanup interval in seconds (default: 1800 = 30 minutes)
# CLEAN_INTERVAL_SECONDS=1800

# Status report interval in seconds (default: 3600 = 1 hour)
# STATUS_INTERVAL_SECONDS=3600

# Node reboot interval in seconds (default: 21600 = 6 hours)
# REBOOT_INTERVAL_SECONDS=21600

# Welcome message sent to new nodes
# WELCOME_MSG=Welcome to Meshtastic!
